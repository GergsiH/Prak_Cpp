program
{
    int a = 0;
    int q = +3, j = -7;
    real b, c =kjhk, d = 2, sdf = 3.6;
    string s = "string", str = 3;

   if(a > b) {
        read(c);
    }
    else write(a);

    while(a < b) a = a + 1;

    for(;;){
        continue;
    }

    for(a = 5; b < a; a = a - 1) read(a);

    c = a + b * 2;
    if (c >= 100 or x == z){
        read (y);
        write (y);
        write (x + y + z, c);
    }
    else
        a = c = 21;

/*     comment 
}